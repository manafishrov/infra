apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # This remains paused until manata is reachable. Unpause only for a supervised
  # backup-and-restore proof, and do not import production mail first.
  - backup.yaml
  - statefulset.yaml
  - services.yaml
  - httproute.yaml
  - network-policy.yaml
  - config.yaml
  - entrypoint.yaml
  - plan.yaml
  - reconciler.yaml
