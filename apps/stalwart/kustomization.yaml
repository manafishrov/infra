apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Enable backup.yaml after manata is reachable and the prepared repository
  # has passed a backup-and-restore test. Do not import production mail first.
  - statefulset.yaml
  - services.yaml
  - httproute.yaml
  - network-policy.yaml
  - config.yaml
  - entrypoint.yaml
  - plan.yaml
  - reconciler.yaml
