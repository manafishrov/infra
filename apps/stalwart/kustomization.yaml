apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Enable backup.yaml after provisioning a dedicated Restic repository/account.
  # Do not import production mail before that backup is verified.
  - statefulset.yaml
  - services.yaml
  - httproute.yaml
  - network-policy.yaml
  - config.yaml
  - entrypoint.yaml
  - plan.yaml
  - reconciler.yaml
