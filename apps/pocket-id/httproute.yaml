apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pocket-id-public
  namespace: manafishrov-pocket-id
spec:
  parentRefs:
    - name: public
      namespace: gateway
      sectionName: https
  hostnames:
    - id.manafishrov.com
  rules:
    - backendRefs:
        - name: pocket-id
          port: 80
